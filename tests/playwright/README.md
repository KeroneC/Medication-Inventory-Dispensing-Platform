# Playwright Tests

The executable Playwright suite lives under `frontend/e2e` so it uses the same `@playwright/test` dependency as the React app.

Run it from `frontend`:

```powershell
npm.cmd run test:e2e
```

This folder remains as a testing-reference area alongside the API and BDD examples.
